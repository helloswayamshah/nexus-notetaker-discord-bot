const { createLogger } = require('../../core/utils/logger');

const log = createLogger('main');

async function start() {
  throw new Error('Slack platform is not yet implemented.');
}

module.exports = { start };